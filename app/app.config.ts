export default defineAppConfig({
  ui: {
    colors: {
      neutral: "zinc",
      primary: "emerald"
    },
    popover: {
      slots: {
        arrow: "fill-current",
        content: "py-2 px-3"
      }
    }
  }
});
